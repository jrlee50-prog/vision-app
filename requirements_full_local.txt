# 로컬(Windows) 풀 기능용 의존성
# Streamlit Cloud 배포에 쓰지 말 것.

opencv-python==4.8.1.78
numpy==1.26.2
pillow==10.1.0
imutils==0.5.4

# 얼굴 인식(무거움: dlib 빌드 필요)
face-recognition==1.3.0
dlib==19.24.2

# 객체 탐지(선택)
ultralytics==8.0.200

# 실시간 비전(선택)
mediapipe==0.10.8
